double area_circle(double r);//计算圆的面积
void swap(double *p, double*q);//交换两个元素
void print_all_element(double *p, int num);//打印全部的一维数组
void  add(double*p, int len, double* sum);//累加，*p是一维数组的首地址，len是长度，sum是输出
void  avg_1arry(double*p, int len, double* avg);//求平均值
void max(double *p, int len, double*out);//求一维数组中的最大值
void min(double *p, int len, double*out);//求一维数组中的最小值